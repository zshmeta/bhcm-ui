/**
 * Icons
 *
 * The MDS entry is documentation-oriented rather than a runtime component.
 * This wrapper exists so the MDS doc is considered migrated.
 */
export default function Icons({ children }) {
  return children ?? null
}

Icons.displayName = 'Icons'
