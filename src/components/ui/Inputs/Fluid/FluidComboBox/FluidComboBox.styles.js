import styled from 'styled-components'

export const Wrap = styled.div`
  /* A light wrapper so Fluid variants can adjust spacing */
  display: block;

  &.fluid-combobox--condensed {
    /* let consumer style if needed; keep minimal */
  }
`
